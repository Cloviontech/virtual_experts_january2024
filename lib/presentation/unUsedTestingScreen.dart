import 'package:flutter/material.dart';

class UnUsedTestingScreen extends StatelessWidget {
  const UnUsedTestingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("For Testing")),
    );
  }
}